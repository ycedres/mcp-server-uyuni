description: "Uyuni MCP Server End-to-End Tests"

evaluateOptions:
  cache: false

defaultTest:
  options:
    provider: "google:gemini-2.5-flash-lite"

prompts:
  - "{{prompt}}"

providers:
  - id: python:mcp_provider.py
    config:
      model: "gemini-2.5-flash-lite"

tests: ../../test/test_cases_sys.py